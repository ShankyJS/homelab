---
# FluxCD-only playbook
# Usage: make deploy-flux

- name: Bootstrap FluxCD
  hosts: servers
  become: true
  vars_files:
    - vault.yml
  vars:
    ansible_become_pass: "{{ vault_become_pass }}"
  roles:
    - flux
