---
# Firewall-only playbook
# Usage: make deploy-firewall

- name: Configure host firewall (UFW)
  hosts: servers
  become: true
  vars:
    ansible_become_pass: "{{ lookup('env', 'BECOME_PASS') }}"
    ansible_ssh_pass: "{{ lookup('env', 'BECOME_PASS') }}"
  roles:
    - firewall
