---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: remote-pi-relay-data
  namespace: remote-pi-relay
  labels:
    app.kubernetes.io/name: remote-pi-relay
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
